/**
 * RESORA — PostCSS Configuration
 * Required by Tailwind CSS (tailwind.config.ts) per Build Prompt
 * Phase 0, Step 1.
 */

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
